# Vercel Marketplace (auto-provisioned via `vercel integration add`)
BLOB_READ_WRITE_TOKEN=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
EDGE_CONFIG=

# Manual — Claude API key for free-tier AI discovery
ANTHROPIC_API_KEY=

# Manual — shared secret for the /api/promote endpoint
PROMOTE_SECRET=

# Manual — GitHub PAT for dead-link sweep to file issues
GITHUB_TOKEN=

# Auto-provisioned by Vercel for cron job auth
# CRON_SECRET=
